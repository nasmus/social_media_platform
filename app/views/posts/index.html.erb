
<body data-controller="reply" class="bg-gray-100 pb-6">
  <div class="max-w-2xl mx-auto">
    <!-- Post Card -->
    <div class="bg-white rounded-lg shadow-sm overflow-hidden">
      <div class="px-4 py-4 border-b-gray-200">
        <%= form_with model: @post, local: true do |form| %>
          <div>
            <%= form.text_area :content, placeholder: "What's on your mind?", class:"block p-5.5 w-full text-xl text-gray-900 bg-gray-50 rounded-lg border border-gray-300 focus:ring-blue-500 focus:border-blue-500"%>
          </div>
          <div class="flex mt-4">
            <%= form.submit "Post Status", class: "px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 hover:cursor-pointer" %>
          </div>
        <% end %>
      </div>
      <!-- Post Header -->
      <div>
        <% @posts.each do |post| %>
          <div class="p-4 border-b border-gray-200">
            <div class="flex items-center space-x-3">
              <img src="https://randomuser.me/api/portraits/women/44.jpg" alt="User avatar" class="w-10 h-10 rounded-full object-cover">
              <div>
                <h3 class="font-semibold text-gray-900"><%= post.user.name %></h3>
                <p class="text-xs text-gray-500">2 hours ago</p>
              </div>
            </div>
          </div>

          <div class="p-4 space-y-2">
            <p class="text-gray-800 leading-relaxed">
              <%= post.content %>
            </p>
          </div>

          <!-- Post Actions -->
          <div class="px-4 py-2 border-t border-gray-200">
            <div class="flex justify-around">
              <button class="flex items-center space-x-1 px-3 py-1.5 rounded-md hover:bg-blue-50 hover:text-blue-600 text-gray-600 hover:cursor-pointer">
                <i class="far fa-thumbs-up"></i>
                <span>Like</span>
              </button>
              <button id="post_comment" data-action="click->reply#cancel" class="flex items-center space-x-1 px-3 py-1.5 rounded-md hover:bg-blue-50 hover:text-blue-600 text-gray-600 hover:cursor-pointer">
                <span>Comment</span>
              </button>
            </div>
            <!-- New Comment Input -->
            <%= form_with(model: [post, Comment.new],local: true) do |f| %>
              <div class="mt-4  reply-input">
                <%= f.text_area :content, placeholder: "Write a comment...", class:"w-full px-3 py-2 text-sm border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent reply-input" %>
                <div class="flex justify-end mt-2">
                  <button class="px-3 py-1.5 mr-2 text-sm text-gray-600 rounded-md hover:bg-gray-100 cancel-reply hover:cursor-pointer ">Cancel</button>
                  <%= f.submit "Post Comment", class: "px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 hover:cursor-pointer" %>
                </div>
              </div>
            <%end%>
            <div class="bg-gray-50 p-4">
              <h4 class="font-bold text-gray-800 mb-3 ">Comments</h4>
              <!-- Comment 2 
              <% post.comments.where(parent_id: nil).each do |comment| %>
                <div class="bg-white rounded-lg shadow-xs p-3">
                  <div class="flex items-center space-x-2 mb-2">
                    <img src="https://randomuser.me/api/portraits/women/68.jpg" alt="Commenter avatar" class="w-8 h-8 rounded-full object-cover">
                    <div>
                      <h5 class="font-semibold text-sm text-gray-900"><%= comment.user.name %></h5>
                      <p class="text-xs text-gray-500">30 minutes ago</p>
                    </div>
                  </div>
                  <p class="text-sm text-gray-700 mb-2"><%= comment.content %></p>
                  <div class="flex space-x-3 text-xs text-gray-500">
                    <button class="hover:underline hover:cursor-pointer">Like (2)</button>
                    <button data-action="click->reply#toggle" class="hover:underline reply-button hover:cursor-pointer">Reply</button>
                  </div>

                              
                    <div data-action="click->reply#toggle" class="mt-3 pl-6 border-l-2 border-gray-200 hidden reply-input">
                      <%= form_with(model: [post, Comment.new], local: true ) do |f| %>
                        <%= f.hidden_field :parent_id, value: comment.id %>
                        <div class="mt-2">
                          <%= f.text_area :content, placeholder: "Write a reply...", class:"w-full px-3 py-2 text-sm border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
                          <div class="flex justify-end space-x-2 mt-2">
                            <button data-action="click->reply#cancel" class="px-3 py-1.5 text-sm text-gray-600 rounded-md hover:bg-gray-100 cancel-reply hover:cursor-pointer">Cancel</button>
                            <%= f.submit "Reply", class:"px-3 py-1.5 bg-blue-600 text-white text-sm font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 hover:cursor-pointer" %>
                          </div>
                        </div>
                      <%end%>
                    </div>
                    
                    <% comment.children.each do |reply| %>
                    <div class="mt-3 pl-6 border-l-2 border-gray-200">
                      <div class="bg-gray-100 rounded-lg shadow-xs p-3">
                        <div class="flex items-center space-x-2 mb-2">
                          <img src="https://randomuser.me/api/portraits/women/44.jpg" alt="Replier avatar" class="w-7 h-7 rounded-full object-cover">
                          <div>
                            <h5 class="font-semibold text-sm text-gray-900"><%= reply.user.name %></h5>
                            <p class="text-xs text-gray-500">45 minutes ago</p>
                          </div>
                        </div>
                        <p class="text-sm text-gray-700 mb-2"><%= reply.content %></p>
                        <div class="flex space-x-3 text-xs text-gray-500">
                          <button class="hover:underline hover:cursor-pointer">Like (1)</button>
                          <button data-action="click->reply#toggle" class="hover:underline reply-button hover:cursor-pointer">Reply</button>
                        </div>

                        
                        <div class="mt-3 pl-6 border-l-2 border-gray-200 hidden reply-input">
                          <div class="mt-2">
                            <%= form_with(model: [post, Comment.new], local: true ) do |f| %>
                              <%= f.hidden_field :parent_id, value: reply.id %>
                              <%= f.text_area :content, placeholder: "Write a reply...", class:"w-full px-3 py-2 text-sm border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
                                <div class="flex justify-end space-x-2 mt-2">
                                  <button data-action="click->reply#cancel" class="px-3 py-1.5 text-sm text-gray-600 rounded-md hover:bg-gray-100 cancel-reply hover:cursor-pointer">Cancel</button>
                                  <%= f.submit "reply", class:"px-3 py-1.5 bg-blue-600 text-white text-sm font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 hover:cursor-pointer" %>
                                </div>
                            <%end%>
                          </div>
                        </div>
                      </div>
                    </div>
                    <%end%>
                  </div>
                </div>
              <%end%>-->
              <% post.comments.where(parent_id: nil).each do |comment| %>
                <%= render partial: "comments/comment", locals: { comment: comment } %>
              <% end %>
            </div>
          </div>

        <% end %>
      </div>
    </div>
  </div>
</body>
